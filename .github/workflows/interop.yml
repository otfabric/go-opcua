name: Interop
run-name: "Interop (${{ github.event_name }}) on ${{ github.ref_name }} by @${{ github.actor }}"

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

permissions:
  contents: read
  packages: read

jobs:
  open62541:
    name: OPC UA interop — open62541
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run interop matrix
        env:
          # Digest-pinned opcua-interop v0.5.0 multi-arch indexes:
          # https://github.com/otfabric/opcua-interop/releases/tag/v0.5.0
          # Current digests: https://github.com/otfabric/opcua-interop/actions/runs/30137524051
          # Set OPEN62541_IMAGE / MILO_IMAGE in the env to test local builds.
          OPEN62541_IMAGE: ghcr.io/otfabric/opcua-interop-open62541@sha256:53c7a184ca9769bab87c6b5335c01322925411894368644913133c0812999283
          MILO_IMAGE: ghcr.io/otfabric/opcua-interop-milo@sha256:c462f45a5e9b9ce8878788b97c77973832b23861e7ae5d0c6642279f798c15cf
          OPCUA_INTEROP_REQUIRE_CAPABILITIES: "1"
        run: make interop

      - name: Collect Docker diagnostics
        if: failure()
        run: |
          mkdir -p artifacts
          docker ps -a > artifacts/docker-ps.txt || true
          docker images --digests > artifacts/docker-images.txt || true

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: go-opcua-interop-diagnostics
          path: artifacts/
          if-no-files-found: ignore
